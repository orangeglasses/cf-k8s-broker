---

applications:
- name: ((appname))   
  stack: cflinuxfs3
  routes:
  - route: ((route))
  processes:
  - type: web
    instances: ((instances))
    memory: 32M
    disk_quota: 500M 
    health-check-type: port
  env:
    GOVERSION: 1.17
 
